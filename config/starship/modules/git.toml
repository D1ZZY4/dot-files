# Git capsules.
[git_branch]
symbol = ""
style = "bg:color_teal fg:color_bg0"
format = "[@LEFT@](color_teal)[ $symbol $branch $git_commit ](fg:color_bg0 bg:color_teal)[@RIGHT@ ](fg:color_teal)"
truncation_length = 32
truncation_symbol = "…"

[git_status]
style = "fg:color_teal"
format = "[$all_status$ahead_behind]($style)"
conflicted = "󰞇 "
ahead = "⇡${count} "
behind = "⇣${count} "
diverged = "⇕⇡${ahead_count}⇣${behind_count} "
untracked = "?${count} "
stashed = "󰏗${count} "
modified = "!${count} "
staged = "+${count} "
renamed = "»${count} "
deleted = "✘${count} "
